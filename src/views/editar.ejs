<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Editar Reserva</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: #f7f7f7;
      color: #333;
      margin: 0;
      padding: 20px;
    }
    h1 {
      text-align: center;
      color: #4a47a3;
    }
    form {
      max-width: 400px;
      margin: 0 auto;
      background: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    input, select {
      width: 100%;
      padding: 10px;
      margin: 8px 0;
      border: 1px solid #ccc;
      border-radius: 4px;
    }
    button {
      background-color: #4a47a3;
      color: white;
      padding: 12px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      width: 100%;
      font-size: 16px;
    }
    button:hover {
      background-color: #36337a;
    }
    a {
      display: block;
      margin-top: 20px;
      text-align: center;
      color: #4a47a3;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <h1>Editar Reserva da Mesa <%= reserva.numeroMesa %></h1>
  <form method="post" action="/reservas/<%= reserva._id %>/editar">
    <input type="text" name="nomeCliente" value="<%= reserva.nomeCliente %>" required>
    <input type="number" name="numeroMesa" value="<%= reserva.numeroMesa %>" required>
    <input type="datetime-local" name="dataHoraReserva" value="<%= reserva.dataHoraReserva.toISOString().slice(0,16) %>" required>
    <select name="status">
      <option value="reservado" <%= reserva.status === 'reservado' ? 'selected' : '' %>>Reservado</option>
      <option value="ocupado" <%= reserva.status === 'ocupado' ? 'selected' : '' %>>Ocupado</option>
      <option value="disponível" <%= reserva.status === 'disponível' ? 'selected' : '' %>>Disponível</option>
    </select>
    <input type="text" name="contatoCliente" value="<%= reserva.contatoCliente %>" required>
    <button type="submit">Salvar Alterações</button>
  </form>
  <a href="/">← Voltar ao painel</a>
</body>
</html>
